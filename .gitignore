# macOS AppleDouble resource-fork files (common on external volumes)
._*
# Python build artifacts
__pycache__/
*.py[cod]
# Editor / OS cruft
.DS_Store
# Local env overrides — holds LAB_GUI_PASSWORD, must never be committed
.env
